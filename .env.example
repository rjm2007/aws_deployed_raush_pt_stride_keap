# Deployed configuration for the Stride/Keap integration box.
# Copy to .env on the server and fill in the real values.

APP_ENV=production
LOG_LEVEL=INFO
LOG_DIR=logs

# --- Database -----------------------------------------------------------
# sslmode=require is enforced when APP_ENV is a deployed environment.
SUPABASE_DB_URL=postgresql://postgres:password@db.example.supabase.co:5432/postgres?sslmode=require

# --- Providers: everything real. Mocks are for local tests only. --------
PROVIDER_MODE=real
VAPI_MODE=real
TWILIO_MODE=real
STRIDE_MODE=real
KEAP_MODE=real

# --- Cadence acceleration ------------------------------------------------
# TEST_MODE only compresses the cadence clock, and only for leads with
# is_test=true. It does NOT switch providers to mocks and does NOT affect
# real leads: those keep the full 14-day cadence, business hours, and caps.
# 1 cadence day = TEST_CADENCE_DAY_MINUTES minutes.
TEST_MODE=true
TEST_CADENCE_DAY_MINUTES=1

# --- URLs ---------------------------------------------------------------
API_BASE_URL=http://api:8000
PUBLIC_BASE_URL=https://stride.aibolt.ai

# --- Vapi ---------------------------------------------------------------
VAPI_BASE_URL=https://api.vapi.ai
VAPI_API_KEY=
VAPI_ASSISTANT_ID=
VAPI_PHONE_NUMBER_ID=
VAPI_WEBHOOK_SECRET=
VAPI_HMAC_SECRET=

# --- Twilio -------------------------------------------------------------
TWILIO_BASE_URL=https://api.twilio.com
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# --- Stride -------------------------------------------------------------
STRIDE_BASE_URL=https://demo.stridethera.com
STRIDE_API_TOKEN=

# --- Keap handoff -------------------------------------------------------
KEAP_HANDOFF_URL=https://keap-team.example.com/events
KEAP_HANDOFF_SECRET=

# --- Signing ------------------------------------------------------------
# Generate with: openssl rand -hex 32
SLOT_TOKEN_SECRET=

# --- Worker and retries -------------------------------------------------
WORKER_POLL_SECONDS=30
DB_POOL_TIMEOUT_SECONDS=5
HTTP_RETRY_ATTEMPTS=3
HTTP_RETRY_BASE_SECONDS=0.5
RETRY_MAX_ATTEMPTS=5
RETRY_BASE_SECONDS=60
RETRY_MAX_SECONDS=3600
